image: python:2.7

pipelines:
  default:
    - step:
        script:
          - pip install setuptools==34.4.1 zc.buildout
          - buildout -n buildout:eggs-directory=../eggs
          - bin/py.test
